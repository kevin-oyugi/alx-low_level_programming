#include "main.h"

/**
 *main: _abs - print absolute value
 *
 *Definition:
 *
 *@n: interger
 *
 *Return: olute interger
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
